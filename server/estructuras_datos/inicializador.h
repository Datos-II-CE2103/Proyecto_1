#ifndef TSTQT_INICIALIZADOR_H
#define TSTQT_INICIALIZADOR_H
/*
#include "linked_list.h"
#include "node.h"
template<class T>

LinkedList* initial(){
    LinkedList* lista = new LinkedList();
    for (int i = 0; i < 5; i++) {
        lista->insertLast(i);
    }
    lista->printList();
    return lista;
}
*/
#endif //TSTQT_INICIALIZADOR_H
